Dijkstras algo using a heap and linked list with runtime O(E lgV) where E = edges and V = verticies.

main.cpp
util.cpp
These two files read user commands and handle them accordingly

heap.cpp
graph.cpp
These two files handle all logic. 